# AIququ-fighting Agent Notes

## 项目定位

AIququ-fighting 当前是一个前后端分离的多人房间制游戏项目。现有产品已经围绕「创建房间、邀请加入、等待、房主开始、游戏流程推进、断线处理、重开确认」建立了可复用链路。下一阶段目标是参考这套多人房间逻辑，讨论并开发一个全新的德扑 PK 游戏。

## 技术栈

- 前端：React 19、TypeScript、Vite、socket.io-client。
- 后端：NestJS、Socket.IO WebSocket Gateway、内存房间状态。
- 部署：前端可部署到 Vercel；后端部署说明中使用 Railway。
- 当前本地状态：`client/src` 源码完整；`server` 目录当前只保留 `dist` 编译产物、`node_modules`、少量配置文件，缺少 `server/src` 与 `server/package.json`。
- 新实现状态：已重建 `server/src` 与 `server/package.json`，新后端编译输出使用 `server/build`，避免覆盖旧 `server/dist` 产物。

## 关键位置

- 前端入口：`client/src/App.tsx`
- 前端全局游戏状态：`client/src/context/GameContext.tsx`
- Socket 封装：`client/src/hooks/useSocket.ts`
- 房间与等待 UI：`client/src/components/Lobby.tsx`
- 牌桌 UI：`client/src/components/PokerTable.tsx`
- 前端类型：`client/src/types/game.ts`
- 后端入口：`server/src/main.ts`
- 后端 WebSocket 网关：`server/src/game/game.gateway.ts`
- 后端房间服务：`server/src/game/room.service.ts`
- 扑克牌组规则：`server/src/poker/deck.ts`
- 标准德扑判定：`server/src/poker/hand-evaluator.ts`
- 后端规则测试：`server/test/*.test.js`

## 现有核心路线

1. 玩家输入昵称，选择创建或加入房间。
2. 创建房间时后端生成 6 位房间号，房主固定为 `red` 方。
3. 加入房间时后端校验房间存在、未满、仍在等待阶段，然后分配下一个阵营。
4. 房主在至少 2 人加入后开始游戏。
5. 当前旧玩法进入主题选择、答题、LLM 评估、展示结果、下一轮或结算。
6. 断线时等待阶段移除玩家；游戏中标记玩家断线，活跃人数不足时取消游戏。
7. 游戏结束后支持房主发起重开，其他玩家确认后重置状态。

## 新德扑 PK 开发原则

- 保留房间邀请、等待、开始、断线、重开这类多人基础链路。
- 旧玩法的主题、答题、LLM 评估、战报展示不应进入新产品核心逻辑。
- 新玩法必须以服务端为唯一权威：洗牌、发牌、换牌、轮次推进、胜负判定都由后端决定。
- 客户端只能发送意图动作，例如抽牌、换牌、确认、弃权或结束操作；不能提交最终手牌或任意牌面。
- 手牌是私密数据，只能通过面向单个 socket 的事件发送给对应玩家。
- 公共牌、玩家操作状态、当前行动者、阶段、倒计时等可以广播给房间。
- 不接入 LLM 或外部 AI API。胜负判定应使用纯规则算法，并通过单元测试覆盖。
- 任何不明确玩法规则，在写实现代码前先讨论并确认。
- 发现 bug 时，先写能够复现的测试，再修复到测试通过。

## UI 与自适应原则

- 现有界面是 desktop-first，再在中小屏折叠。
- 新扑克产品应优先保证桌面牌桌信息密度，同时移动端可完整操作。
- 公共牌、玩家席位、手牌、当前可操作按钮应有稳定尺寸，避免阶段切换导致布局跳动。
- 私密手牌与公开信息必须视觉分区清楚。
- 换牌操作中的选中态必须有高对比边框、位移或发光反馈，让玩家一眼确认当前选择。
- 结算阶段必须先按本局行动顺序逐个亮出玩家手牌和牌型，全部亮完后再展示按牌力排序的最终排名。
- 结算展示必须区分「手牌」「最佳五张」「未参与暂存牌」；暂存区剩余牌不参与 7 选 5 判定，只在结算后公开展示。
- 获胜玩家需要在结算面板中有明确的胜利标识和奖励动效。
- 发牌、抽牌、换牌、弃牌和玩家轮转都要有可感知动效或状态提示；动效要帮助玩家识别阶段和行动者，不能遮挡公共牌、手牌或行动按钮。
- 公共动作摘要只能广播安全信息，例如动作类型、行动玩家、下一位玩家、轮次和公开弃牌线索；不能泄露其他玩家的手牌或暂存牌牌面。
- 等待页中已占据座位和自己的座位必须明显区别于空座，禁用的已入座座位也不能因为默认 disabled 透明度而显得像不可读状态。
- 游戏中必须提供玩法说明入口，说明发牌、换牌、暂存区、最终 7 选 5 和并列获胜规则。
- 最终亮牌不能直接整体展示排名；必须先按行动顺序逐个展示玩家手牌、最佳五张和牌型，并在最后一名玩家展示后停留一拍，再切换到最终排名和胜者动效。

## 文档维护规则

- 每次讨论、需求澄清、实现变更或问题修复后，更新 `lessons.md`。
- `lesson.md` 保留为单数文件入口，指向 `lessons.md`。
- 新产品方案讨论文档放在 `docs/` 下，进入实现前先确认方案。

## 已确认的德扑 PK 规则

- 支持 2 到 6 人游玩。
- 房主可设置连续游戏次数，范围为 1 到 5 次。
- 房间不变时，持续统计每个玩家在该房间内的获胜次数。
- 玩家离开房间后再加入，按新玩家重新统计胜场。
- 玩家在等待页可以选择座位；座位编号可见，并按逆时针顺序编排。
- 每次游戏使用无大小王的 52 张标准扑克牌。
- 开局给每名玩家发 2 张私密手牌，并发 5 张所有人可见的公共牌。
- 未发出的牌进入待抽取区域。
- 换牌环节共 5 轮，从随机玩家开始，按逆时针顺序行动。
- 每名玩家每轮抽 1 张牌进入自己的暂存区，暂存区只对自己可见。
- 每回合 30 秒行动时间；超时默认不操作。
- 每回合只能选择不操作、置换手牌、置换公共牌三种行动之一。
- 选择任一行动后自动结束回合。
- 置换手牌产生的弃牌任何玩家都不可见，包括弃牌玩家本人；置换公共牌产生的弃牌所有玩家可见。
- 所有玩家完成 5 轮后，按行动顺序依次亮手牌并展示牌型，再展示排名和获胜标识。
- 最终比拼采用标准德州扑克 7 选 5：2 张手牌加 5 张公共牌，选最佳 5 张比较。
- 标准德州扑克比较后允许并列第一，并列第一玩家都获得获胜次数 +1。
- 结算后公开展示所有玩家未使用的暂存区牌。
- 如果一组游戏中还有下一次游戏，需要所有在线玩家点击继续。
- 游戏中玩家断线时保留座位，轮到断线玩家时按超时不操作处理；如果只剩 1 名玩家在线，取消本次游戏且不计胜场。
- 达到房主设定次数后回到房间和座位页面；房主可重新设置次数并再次开始。

## 本地运行

- 后端构建：`server\node_modules\.bin\tsc.cmd -p server\tsconfig.json`
- 后端测试：`node --test server/test/*.test.js`
- 后端启动：在 `server` 目录执行 `node build/main.js`
- 前端类型检查：`client\node_modules\.bin\tsc.cmd -p client\tsconfig.json --noEmit`
- 前端开发：在 `client` 目录执行 `node_modules\.bin\vite.cmd --host 127.0.0.1`
- 测试地址：`http://127.0.0.1:5173/`

## 独立项目提交原则

- 当前仓库作为独立的德扑 PK 项目维护，不再提交旧 AIququ 答题、主题、LLM、DeepSeek 或战报源码。
- 前端包名使用 `texas-poker-pk-client`，后端包名使用 `texas-poker-pk-server`。
- `server/src` 只保留德扑房间、Socket 网关、扑克牌组和牌型判定相关源码。
- 部署文档不得要求 AI API Key；只需要前端 `VITE_SOCKET_URL` 指向后端服务地址。
