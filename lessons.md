# Lessons

## 2026-05-19 德扑 PK 新产品启动

- 原因/背景：用户希望参考现有多人房间邀请逻辑，开发一个全新的德扑 PK 游戏；旧产品玩法与新玩法无关。
- 纠正/处理：当前阶段只做代码审查与方案文档，不进入玩法实现；新玩法细节需要用户后续完整描述并确认。
- 防止规则：当需求中的玩法规则尚未给出时，不编写游戏逻辑代码，只整理架构边界、可复用链路和待确认问题。

## 2026-05-19 后端源码缺失风险

- 原因/背景：本地 `server` 目录没有 `src` 和 `package.json`，只有 `dist` 编译产物、依赖与配置。
- 纠正/处理：本次仅从 `server/dist/game/*.js` 反推后端逻辑，用于方案讨论。
- 防止规则：进入开发前应先恢复或重新建立后端源码结构，避免长期直接修改编译产物。

## 2026-05-19 新产品不继承旧 LLM 玩法

- 原因/背景：旧产品通过 DeepSeek API 做答题评分和战报生成，新德扑 PK 明确不需要 LLM 或 API 调用。
- 纠正/处理：方案中将旧的 `LlmService`、主题选择、答题、评估、战报逻辑标记为不可复用。
- 防止规则：德扑 PK 的洗牌、发牌、换牌、胜负判定全部使用服务端纯规则算法，并用测试约束。

## 2026-05-19 德扑 PK 玩法规则明确

- 原因/背景：用户补充了完整玩法，包括 2-6 人、1-5 次游戏、52 张牌、2 张手牌、5 张公共牌、5 轮换牌、30 秒回合倒计时、亮牌结算和房间内胜场统计。
- 纠正/处理：已将玩法细则更新到 `docs/texas-poker-pk-discussion.md`，并同步到项目级 `AIququ-fighting_agent.md`。
- 防止规则：后续实现必须按「服务端权威发牌/抽牌/换牌/结算」设计，客户端只提交行动意图，不能提交或决定牌面。

## 2026-05-19 牌型判定规则确认

- 原因/背景：用户确认最终比拼采用标准德州扑克牌型判定。
- 纠正/处理：已将「2 张手牌 + 5 张公共牌，7 选 5」固化进 `docs/texas-poker-pk-discussion.md` 和 `AIququ-fighting_agent.md`。
- 防止规则：实现 `hand-evaluator` 时必须按标准德州扑克 7 选 5 比较，不能使用只比较 2 张手牌或自定义牌型的简化逻辑。

## 2026-05-19 平局胜场规则确认

- 原因/背景：用户确认允许并列玩家都获胜次数 +1 的平局出现。
- 纠正/处理：已将并列第一的胜场统计规则写入 `docs/texas-poker-pk-discussion.md` 和 `AIququ-fighting_agent.md`。
- 防止规则：结算逻辑不能强行拆分完全平局；标准德州扑克比较后若存在并列第一，应为所有并列第一玩家增加房间内获胜次数。

## 2026-05-19 玩法边界规则集中确认

- 原因/背景：用户统一确认了断线、只剩 1 人在线、座位选择、逆时针顺序、多局继续、自动亮牌、暂存区公开、私密弃牌隐藏、自动结束回合、重新加入胜场重置等边界规则。
- 纠正/处理：已将 `docs/texas-poker-pk-discussion.md` 中的旧问题清单改为「已确认边界规则」，并同步更新 `AIququ-fighting_agent.md`。
- 防止规则：实现时不要再引入自动分配座位、断线立即判负、结算后隐藏暂存区、玩家可查看自己的私密弃牌、行动后还需额外点击结束等相反逻辑。

## 2026-05-19 德扑 PK 开发完成

- 原因/背景：用户确认方案后要求进入开发，并希望完成后可本地测试。
- 纠正/处理：已重建后端源码与 `server/package.json`，新增扑克规则测试，改造 Socket 网关、房间服务、前端状态、等待页选座和牌桌 UI。新后端编译输出放在 `server/build`，避免覆盖旧 `server/dist`。
- 防止规则：后续验证使用 `node --test server/test/*.test.js`、`server\node_modules\.bin\tsc.cmd -p server\tsconfig.json`、`client\node_modules\.bin\tsc.cmd -p client\tsconfig.json --noEmit` 和 Vite build；本地测试先启动后端 3000，再启动前端 5173。

## 2026-05-19 牌桌结算与选牌反馈优化

- 原因/背景：测试反馈显示选中牌的视觉反馈不够强；最终结算中手牌、最佳五张和暂存区未参与牌容易混淆；座位信息展示了不必要的暂存数量；亮牌过程和获胜表现不够明确。
- 纠正/处理：增强牌选中态的边框、发光、位移和“已选”标签；公开座位状态移除 `stashCardCount`；结算结果改为区分 `holeCards`、`bestCards`、`unusedStashCards`；亮牌阶段按本局行动顺序逐个显示，完成后展示排名顺序，并为胜利方添加 banner 与动效。
- 防止规则：暂存区剩余牌不能进入最终 7 选 5 判定，只能在结算后作为未参与牌公开展示；结算 UI 必须同时验收“行动顺序亮牌”“牌型文本展示”“最终排名”“胜者动效”四项。

## 2026-05-20 牌局过程动效与行动切换提示

- 原因/背景：用户反馈希望每一步发牌、换牌、弃牌和玩家切换都有更清楚的动效，让自己和其他玩家都能明确看到当前阶段与行动者。
- 纠正/处理：新增服务端公开 `lastAction` 动作摘要，用测试约束其包含轮次、动作类型、行动玩家和下一位玩家；前端增加阶段提示条、行动席位脉冲、牌堆/弃牌计数跳动、公共牌发牌/换牌、手牌接收、暂存抽牌和公开弃牌动效。
- 防止规则：过程动效依赖服务端权威状态，不在客户端推断牌局结果；公开动作摘要不得泄露其他玩家的手牌或暂存牌具体牌面；动效必须保留 reduced-motion 降级。

## 2026-05-20 等待页座位、玩法说明与亮牌节奏修正

- 原因/背景：用户指出等待页已占据座位和自己的座位不够突出；游戏中缺少玩法说明入口；最终结算仍像整体一次性展示，没有形成逐个玩家亮牌和展示组合的观感。
- 纠正/处理：等待页座位增加 `occupied`、`mine`、`empty` 分层样式和“已入座/我的座位”标记，并覆盖 disabled 透明度；牌桌顶部增加玩法说明弹窗；抽出 `getShowdownRevealState` 并添加测试，亮牌流程改为准备阶段、按行动顺序逐个展示、最后一名停留一拍、再进入最终排名。
- 防止规则：结算亮牌的完成状态必须比玩家数量多一拍，不能在最后一名玩家刚出现时立即切到完整排名；等待页禁用座位仍需保持可读；玩法说明内容应随规则变更同步更新。

## 2026-05-20 独立项目化提交

- 原因/背景：用户希望把当前德扑 PK 与原本 AIququ 项目区隔开，作为独立项目提交到 git。
- 纠正/处理：清理未引用的旧答题/主题/LLM 后端源码，前端包名改为 `texas-poker-pk-client`，新增独立项目 `README.md`，重写部署指南为不需要 AI API Key 的德扑 PK 部署说明。
- 防止规则：独立项目提交前必须搜索旧项目关键词，例如 `gomoku-client`、`DeepSeek`、`LlmService`、答题、主题、战报；源码与部署文档不得继续依赖旧 AIququ 玩法。
